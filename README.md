# Monitor - Django + psutil

Aplicación Django simple que muestra el estado del sistema en tiempo real usando `psutil`.

## Requisitos
- Python 3.8+
- pip

## Instalación

1. Clona o descarga el proyecto.
2. Crea y activa un entorno virtual (recomendado):
   ```bash
   python -m venv venv
   venv\Scripts\activate        # Windows
   # source venv/bin/activate   # Linux/macOS